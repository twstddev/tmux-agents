# tmux-agents

A lightweight tmux plugin for tracking Codex and Claude Code agents across sessions.

It will show compact agent-state counts, provide an fzf chooser with pane previews, and jump directly to agents that need attention.
